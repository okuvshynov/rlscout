```
[x] rename the library
[ ] decouple othello-specific functionality
[ ] move all python code to src. 
    [x]For model: we need to clean-up model DB and test everything from scratch anyway.
[ ] unit tests for both C++ and python parts
    [x] at least build automatically at first 
[ ] integration tests 
    [ ] self-play with/without the model
[?] add requirements.txt
    [ ] need to test more. Different environment need different versions, for example raspberry pi has to use older torch.
[ ] use compiple-time config.
[ ] signal to shutdown everything
```